---
description: Diretivas de stack Cursor — Kairogen off, Stitch diário, Actionize arquivado
alwaysApply: true
---

# Stack Cursor — diretivas TheMasterBECK (2026-08-22)

## Execucao (PRIORIDADE ABSOLUTA)
- **NUNCA** pedir TheMasterBECK para acoes mecanicas.
- Rule: `agent-executes-all.mdc` (`alwaysApply`) + injecao no hook `sessionStart`.

## Prompt skills (autorun global)
- **SEMPRE ativos:** prompt-engineering + context-engineering (B, economia tokens) + `/prompt-skills-autorun`
- Status: `Autorun ativo: prompt-engineering + context-engineering (B, economia tokens) + prompt-skills-autorun`
- **FORA do autorun:** enhance-prompt (somente Stitch / `/stitch-diario`)
- Rule: `prompt-skills-autorun.mdc`
- Command: `/prompt-skills-autorun`
- Hook: `sessionStart` → `hooks/prompt-skills-autorun.js` (+ guard `afterAgentResponse`/`stop` em `agent-executes-*.js`)
- Controle: subagente `patroa`; herança nos 5 `~/.cursor/agents/*.md`

## Kairogen
- **DESLIGADO** até segunda ordem.
- Config preservada em `~/.cursor/mcp.kairogen.disabled.json`.

## Stitch (fluxo diário)
- MCP `stitch` via `pnpm.cmd dlx stitch-mcp-stdio` + `STITCH_API_KEY`
- Slash: `/stitch-diario` (aqui sim: enhance-prompt no pipeline)
- Skills: `~/.claude/skills/stitch/`

## Google Workspace
- `gmail` + `google-calendar` autenticados no mcp.json
- Plugins locais em `~/.cursor/plugins/local/`

## Catálogo
- READY: premiere, notebooklm, mercadopago (+ github, higgsfield, chrome, playwright)
- ERROR → `mcp.error.disabled.json`: bling, photoshop

## Actionize
- **ARQUIVADO**. Ver `actionize-archived.mdc`.

## Agents
- `gatona`, `patroa`, `querida` em `~/.cursor/agents/`
